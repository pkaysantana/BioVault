# Copy to .env.local for local overrides (not committed).
# Local dev: point at uvicorn. Unified Vercel deploy (root vercel.json) uses /api by default in production.
VITE_API_BASE_URL=http://localhost:8000
