[project]
name = "biovault-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "cryptography>=49.0.0",
  "fastapi>=0.138.0",
  "python-dotenv>=1.2.0",
  "uvicorn>=0.49.0",
]

[tool.vercel]
entrypoint = "app.main:app"
